  <p id="notice"></p>
  <h2>User info</h2>
    <%= attachment_image_tag user,:profile_image, format: 'jpeg', fallback: "noimage.png", size: "100x100" %>
  <table class="table">
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  </table>
<%= link_to edit_user_path(user), class: 'btn btn-outline-secondary btn-block' do %>
<i class="fas fa-user-cog"></i>
<% end %>
